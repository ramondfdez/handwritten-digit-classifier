[project]
name = "handwrittenpics"
version = "0.1.0"
description = "Handwritten digit classifier from image input"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "numpy>=1.24",
  "pillow>=10.0",
  "scikit-learn>=1.4",
]

[tool.uv]
package = false
